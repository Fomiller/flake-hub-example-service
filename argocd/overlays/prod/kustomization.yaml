# ---------------------------------------------------------------------------
# GENERATED FILE — managed by flake-hub (golden-argocd).
# Do not edit manually: `nix run .#generate` overwrites it.
# To change it, edit repo.nix, or the template in the pack that owns it.
# ---------------------------------------------------------------------------

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: flake-hub-example-service-chart
    repo: oci://000000000000.dkr.ecr.us-east-1.amazonaws.com
    version: 0.1.0
    releaseName: flake-hub-example-service-prod
    namespace: flake-hub-example-service
    # Shared defaults first, this environment's overrides second. On a key both
    # files set, the later one wins.
    valuesFile: ../values.app.base.yaml
    additionalValuesFiles:
      - values.app.yaml
