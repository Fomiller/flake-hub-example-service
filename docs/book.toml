# ---------------------------------------------------------------------------
# GENERATED FILE — managed by flake-hub (golden-docs).
# Do not edit manually: `nix run .#generate` overwrites it.
# To change it, edit repo.nix, or the template in the pack that owns it.
# ---------------------------------------------------------------------------

[book]
authors = ["Forrest Miller"]
language = "en"
src = "src"
title = "flake-hub-example-service"

[output.html]
additional-css = ["./theme/catppuccin.css"]
# Both, so the book looks the same whatever the reader's OS is set to.
default-theme = "frappe"
preferred-dark-theme = "frappe"
git-repository-url = "https://github.com/Fomiller/flake-hub-example-service"
edit-url-template = "https://github.com/Fomiller/flake-hub-example-service/edit/main/docs/{path}"

[output.html.fold]
enable = true
level = 1
