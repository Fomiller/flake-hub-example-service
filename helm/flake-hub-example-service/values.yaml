# Written once, then it is yours. `nix run .#generate` never touches it again.

replicas: 2

image:
  repository: flake-hub-example-service
  tag: latest

# Empty renders no resources block at all. The deployed overlay sets them.
resources: {}
